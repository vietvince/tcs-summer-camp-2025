---
title: "Styling the Cookie & Containers"
sidebar_position: 4
toc_min_heading_level: 2
toc_max_heading_level: 3
---

:::info
<details style={{border:'none'}}>
  <summary>**Prerequisites: Before You Dive In!**</summary>

  - You have completed **Cookie Clicker 1 – Project Setup & HTML Structure**.  
  - Your project contains **index.html** and an empty **css/style.css**.  
  - You have placed `cookie.png` in the `images/` folder.  
</details>
:::

## Objective 🧐

Use the supplied CSS rules to style your cookie clicker interface: the save/load buttons, the cookie display, and the shop containers.

## Getting Started 🚩

### Step 1. Layout Styles

1. Open **css/style.css** and add:

```css
h1 {
    margin-bottom: 0px;
}
```
- This removes default body margins.

### Step 2. Save/Load Controls

1. Below your base styles, paste:

```css
.saving {
    position: absolute;
    width: 100%;
    left: 5px;
}
.saving button {
    width: 100px;
    float: left;
    border: 2px solid rgb(0, 0, 0);
    border-top: none;
    border-radius: 0px 0px 5px 5px;
    margin-top: -2px;
    height: 27px;
    line-height: 25px;
    outline: none;
}
.saving button.import {
    color: rgb(255, 255, 255);
    background-color: rgb(33, 171, 12);
    border-color: rgb(27, 138, 10);
    box-shadow: 0px 3px rgb(18, 87, 7);
    border-right: none;
    border-radius: 0px 0px 0px 5px;
}
.saving button.export {
    color: rgb(255, 255, 255);
    background-color: rgb(158, 29, 29);
    border-color: rgb(136, 25, 25);
    box-shadow: 0px 3px rgb(92, 16, 16);
    border-left: none;
    border-radius: 0px 0px 5px 0px;
}
.saving button.export.wipe {
    position: absolute;
    right: 10px;
    border-radius: 0px 0px 5px 5px;
    border-left: 2px solid rgb(136, 25, 25);
}
```
- These rules style the top‐bar buttons, including hover/active shadows (hover/active rules come later).

### Step 3. Import/Export Overlay

1. Still in style.css, add:

```css
.importExportBox {
    position: absolute;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.4);
    visibility: hidden;
}
.importExportBox div {
    width: 300px;
    height: 350px;
    position: absolute;
    top: 50%;
    left: 50%;
    margin-top: -175px;
    margin-left: -150px;
    background-color: rgb(0, 0, 0);
    border-radius: 5px;
}
.importExportBox div textarea {
    display: block;
    width: 282.5px;
    height: 290px;
    margin: 7.5px;
    border: none;
    border-radius: 3px;
    outline: none;
    resize: none;
}
.importExportBox div button {
    width: 150px;
    height: 30px;
    margin: 7.5px auto;
    display: block;
    border-radius: 5px;
    outline: none;
    color: white;
    background-color: rgb(158, 29, 29);
    border-color: rgb(136, 25, 25);
    box-shadow: 0px 2px rgb(92, 16, 16);
}
```
- These styles create the semi-transparent import/export modal and center its contents.

### Step 4. Cookie & Counter

1. Next, style the main game area:

```css
.cookieContainer {
    width: 100%;
    height: 310px;
    text-align: center;
}
.cookieButton {
    background-color: transparent;
    border: none;
    outline: none;
    padding: 0;
    margin: auto;
    display: block;
}
.cookieButton img {
    width: 300px;
    height: 300px;
    transition: transform 0.1s ease;
}
.cookieButton img:hover {
    transform: translateY(2.5px);
}
.cookieButton img:active {
    transform: translateY(5px);
}
.cookieDisplay {
    font-size: 24px;
    user-select: none;
}
.cpsDisplay, .cpcDisplay {
    font-weight: bold;
    user-select: none;
}
```
- This centers your cookie, adds click animations, and styles the count displays.

### Step 5. Shop Layout

1. Finally, add shop styles:

```css
.shopList {
    width: 259px;
    float: left;
    clear: both;
}
.shopList button {
    display: block;
    width: 85%;
    max-width: 220px;
    margin: auto 0 5px;
    min-height: 35px;
    color: white;
    background-color: rgb(66, 135, 245);
    border: 2px solid rgb(38, 89, 171);
    border-radius: 5px;
    outline: none;
    box-shadow: 0px 3px rgb(22, 59, 120);
}
.btnBuyGroup button {
    width: 120px;
    margin: 0 5px 7px 0;
    background-color: rgb(33, 171, 12);
    border: 2px solid rgb(27, 138, 10);
    border-radius: 5px;
    color: white;
    box-shadow: 0px 3px rgb(18, 87, 7);
}
.shopContainer {
    margin-top: 30px;
    overflow: auto;
}
```
- These rules arrange your upgrade buttons and building panels.
<br />

Great styling! Your Cookie Clicker now looks polished and interactive.

Next up: **Implementing Click Logic & Counter Update**, where we’ll hook up your JavaScript to make clicks count!